{
  "name": "wa-bot",
  "version": "1.0.0",
  "description": "WhatsApp auto-sender bot — cloud version",
  "main": "wa.js",
  "scripts": {
    "start": "chmod +x start.sh && ./start.sh"
  },
  "dependencies": {
    "whatsapp-web.js": "*",
    "express": "*",
    "qrcode-terminal": "*",
    "puppeteer": "*"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
